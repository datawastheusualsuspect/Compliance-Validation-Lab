filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/nginx/*.log
    - /usr/local/apache2/logs/*

output.elasticsearch:
  hosts: ["http://elk:9200"]

setup.kibana:
  host: "http://elk:5601"
